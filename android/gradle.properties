org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Disable Gradle VFS watching to prevent file locking conflicts
org.gradle.vfs.watch=false
# Disable Gradle daemon to prevent leftover processes from locking cache files
org.gradle.daemon=false
# Force single-threaded execution to prevent concurrent transform file move conflicts (Gradle bug on Windows)
org.gradle.parallel=false
org.gradle.workers.max=1
org.gradle.configuration-cache=false
